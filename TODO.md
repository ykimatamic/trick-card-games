# TODO: カードゲームスイート拡張

- [x] 神経衰弱の2手目はやすぎ
  - 2枚めくり後に 800ms の間隔を挟んでからペア判定する `pending`/`resolve` フローへ変更。2枚目をめくっても即座に伏せず、一定時間カードを見せてから確定する。
- [x] ババ抜きのジョーカーの位置絶対1番右じゃね？
  - 配布＋ペア除去後に各手札をシャッフルし、ジョーカーが常に一番右に配置されないようにした(位置が露出しない)。回帰テスト追加。
- [x] 7並べパス無制限はだめでしょ。
  - 出せるカードがある間はパス不可に変更。パスは「出せるカードが無い場合」のみ許可(無制限パスによる停滞を防止)。
- [x] ポーカー、手の開示ないと。しかも役の強さおかしそう。ワンゲームじゃおもしろくない。
  - ショウダウンで全員の手札(カード)を開示表示するようにした。
  - 役の強さ判定のバグを修正(`valueOf` が役種ごとの構成数で桁が変わり、フォーカード等が上位役を上回る問題を解消)。
  - 単ハンドで終わらず、チップを持ち越して複数ハンドを自動継続するマルチハンド戦(連戦)を実装。破産 or 上限ハンド数でマッチ終了。

## バグ(対応済み)

- [x] ブラックジャック: プッシュ(引き分け)でディーラーが負け扱いになる
  - `blackjack.ts` の `results`。`outcome === 'push'` を早期リターンで両者 `{rank:1, isLoser:false}` として扱う(プッシュで誰も負けない)。回帰テスト追加。

- [x] ポーカー CPUの交換ロジック: 4枚フラッシュ/ストレート構想なのに4枚捨てている
  - `poker.ts` の `cpuDiscard`。フラッシュ/ストレート構想ブランチの `__weakDiscard(cards, 1)` を `__weakDiscard(cards, 4)`(keep=4, discard=1)に修正。

- [x] ポーカー: レイズの正当性検証が上限キャップ前に通ってしまう
  - `poker.ts` のレイズ。キャップ(`newBet = maxNewBet`)後に `newBet <= currentBet` を再検証して返すことで、ショートスタックでの `currentBet` 引き下げ/状態破壊を防止。

- [x] ポーカー: 0枚交換(スタンドパット)が選択不可で強制交換になる
  - `PokerView.tsx` の「交換」ボタンから `disabled={discarding.size === 0}` を除去し、0枚交換を許可。

- [x] ブラックジャック: 伏せたホールカードまで含む合計が表示され情報漏洩
  - `BlackjackView.tsx` に `visibleHandValue`(表向きカードのみ合計)を追加し、ディーラー合計表示に使用。

## 改善・その他(対応済み)

- [x] `contract.ts:119-127` の `play()` が常に `true` を返す
  - `applyAction` を `boolean` を返すように変更し、`play` はその結果を伝播。不正着手を成功扱いしない。

- [x] 神経衰弱: 不一致時に次プレイヤーの「成功音」が鳴る
  - `MemoryView.tsx`。`next.currentPlayer === prev.currentPlayer`(ペア成立・手番継続)のときのみ `playFoundation`、不一致時は `playError` を再生。

- [x] 神経衰弱: 自動解決 `useEffect` の依存配列が不安定
  - `MemoryView.tsx`。依存を `[state, host.currentPlayer, host.play]`(`host.play` は安定)に変更し、`host` オブジェクト全体を依存から除外(意図的な除外はコメントで明記)。

- [x] ポーカー: 終了画面が順位表を表示しない
  - `PokerView.tsx`。`host.finished` オーバーレイで `host.results` を順位リスト(`pk-standings`)として表示。フォールド決着時にも最終順位が見える。

- [x] 7並べ: 全員が出せない状況でパスが無限ループし得る
  - `sevens.ts`。`SevensState.consecutivePasses` を追加し、全周(残り全員)連続パスで詰みと判定して手札の少ない順に残り順位を確定し終了。回帰テスト追加。

- [x] ゲーム別の isHuman 判定方法が不統一
  - `SevensView.tsx` / `DaifugoView.tsx` を他ビュー同様 `p.id === HUMAN` に統一。

- [x] ババ抜き: 引いたカードが何か直後に開示されない
  - `OldMaidView.tsx`。引いたカードを表向きで一時表示し、約850ms後に自動解決(ペア即消滅前に確認できる)。

- [x] ポーカー: `broke`(破産フラグ)が一度も代入されない
  - `poker.ts`。`pay` とアンティ支払い後にチップ0になったプレイヤーへ `broke` を設定。回帰テスト追加。

- [x] ブラックジャック: 山札切れの防御ガード無し
  - `blackjack.ts`。`draw()` を `[MpCard | null, MpCard[]]` を返すようにし、全呼び出し側が空札を検査して null を返す。

- [x] 神経衰弱: 同数で複数人が並んだときの順位の扱い
  - `memory.ts` の `results`。獲得枚数で競技式順位(同数は同順位: 1,2,2,4)を付与。回帰テスト追加。

- [x] 大富豪/7並べ: `playerCount <= 0` で `done` にならず `currentPlayer` が -1 になる
  - `daifugo.ts` / `sevens.ts`。`playerCount <= 0` で即座に `done: true` の空状態を返す(配布の除算/配列アクセスも防ぐ)。

- [x] ババ抜き: セットアップ時点で手札0の複数プレイヤーが全員「順位付き」で並び、最後の1人が敗者化
  - `oldmaid.ts`。セットアップ時点で残り1人(ババ持ち)まで確定した場合は `order` 末尾に追加し、敗者は必ずババ持ちにする。回帰テスト追加。
